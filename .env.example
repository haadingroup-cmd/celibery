# The deployed Celibery backend's origin (haadingroup-cmd/celibery11),
# no trailing slash. Checkout, the contact form, and newsletter signup
# call this API; without it they fail with a "not connected" error.
NEXT_PUBLIC_API_URL=https://celibery11.vercel.app
